# ⚠️ Use `sheet-master.csv` instead

**One file only:** import **`sheet-master.csv`** — see **`docs/SHEET-MASTER-GUIDE.md`**.

The older two-file setup (`sheet-input-template.csv` + `sheet-lookup-tables.csv`) is replaced by the single master sheet with lookup rules on the right (columns AU–BH).
